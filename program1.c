#include <stdio.h>

int main(void) {
	int input;
	 scanf("%d",&input);
	 if(input>0)
	  printf("entered input is positive!!!");
	  else if(input>0)
	      printf("entered input is negative!!!");
	      else
	         printf("entered input is zero!!!");
	 
	
	return 0;
}
